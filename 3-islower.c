#include "main.h"
/**
 * _islower - program that checks for lowercase characters
 * @c: character to check
 * Return: 1 if c is lowercase, 0  if not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
